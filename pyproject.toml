[project]
name = "dagster-test"
version = "0.1.0"
description = "Universal Agentic Framework Test"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "dagster",
    "dagster-webserver",
    "pandas",
    "tabulate",
    "pymupdf",
    "requests",
]
